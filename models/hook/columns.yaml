# ===================================
#  List Column Definitions
# ===================================

columns:
    name:
        label: wiz.webhooks::lang.hooks.columns.name

    executed_at:
        label: wiz.webhooks::lang.hooks.columns.executed_at
        type: timesince

    logs_count:
        label: wiz.webhooks::lang.hooks.columns.logs_count
        type: number

    url:
        cssClass: url-column
        label: wiz.webhooks::lang.hooks.columns.url
        type: partial
        path: $/wiz/webhooks/models/hook/_url.htm
        clickable: false

    is_enabled:
        label: wiz.webhooks::lang.hooks.columns.status
        type: partial
        path: $/wiz/webhooks/models/hook/_status.htm

    execute_btn:
        cssClass: execute-btn
        label: wiz.webhooks::lang.hooks.columns.execute_btn
        type: partial
        path: $/wiz/webhooks/models/hook/_execute.htm
        sortable: false
        clickable: false
